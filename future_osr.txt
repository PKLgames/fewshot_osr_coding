OSR17 重训行动指南 — 基于最新文献启发的全面重构
================================================

Baseline: FOAC-AIFP (华南理工, Li & Tan)
  代码位置: /coding/FOAC-AIFP/
  已有模型: save_tau22/ (pretrain + 3个best模型: acc/auroc/fscore)
  Backbone: ResNet-18 (ImageNet预训练 → Log-Mel频谱 → 512-dim)
  OSR方法: RPL互反点预训练 + NPM开放集生成器 + (N+1)-way分类
  训练策略: 两阶段 (RPL/GCPL预训练 → Episodic元训练)
  评估指标: ACC (闭集), AUROC (开放集), F-score
  Episode配置: 2-way 5-shot 15-query (TAU22仅6基类，受限)

目标: 在FOAC-AIFP baseline之上，融合SupCon特征学习、可学习阈值、
      互反点增强、Curriculum OOD等思想，实现OSR性能跃升
前提: 需从头训练完整 Phase 0→1→2→3 流水线

与FOAC-AIFP的关键区别:
  1. Backbone: YAMNet(64-dim) vs ResNet-18(512-dim) → 我们的更轻量
  2. 特征学习: SupCon(有监督对比) vs RPL+GCPL → 更强的类间分离
  3. OSR策略: 多方法融合 vs 单一(N+1)-way → 更鲁棒
  4. Episode规模: 5-way vs 2-way → 我们评估更充分

实施路径: osr17a (特征升级+可学习阈值) → osr17b (互反点+curriculum OOD) → osr17c (不确定性建模)


════════════════════════════════════════════════════════════════════
零、Baseline (FOAC-AIFP) 先行运行
════════════════════════════════════════════════════════════════════

在开始osr17之前，先确保FOAC-AIFP baseline在TAU22上能跑出结果，
作为论文对比实验的基准数据。

已有模型 (save_tau22/):
  pretrain_model_tau22.pth  — RPL/GCPL预训练权重
  model_TAU22_max_acc.pth   — 最佳ACC模型
  model_TAU22_max_auroc.pth — 最佳AUROC模型
  model_TAU22_max_fscore.pth — 最佳F-score模型

运行测试获取baseline指标:
  cd /coding/FOAC-AIFP
  python main.py --config default.yml --pretrain False --test True

记录结果 (填入论文对比表):
  □ ACC (闭集分类准确率)
  □ AUROC (开放集检测曲线下面积)
  □ F-score (宏平均F1)

注意: FOAC-AIFP的Episode是2-way (受限于6基类)，
      我们的方法用5-way，对比时需注明Episode配置差异。
      若需公平对比，可调整我们方法的N_way=2跑一组。


════════════════════════════════════════════════════════════════════
一、osr17a: SupCon特征升级 + 可学习OSR阈值 (优先级1+2)
════════════════════════════════════════════════════════════════════

预期收益: 特征质量全面提升 → 所有下游OSR方法受益
工作量: 小 (改ContrastivePretrainer + 新增LearnableThreshold)
预计耗时: Phase 0约2h + Phase 2约2h = ~4h

────────────────────────────────────────
1.1 Phase 0a: SupCon替代SimCLR
────────────────────────────────────────

原理: SimCLR不知道类别标签，同类样本可能被推远。
      SupCon利用已知类标签(仅base classes)，确保同类紧凑、异类远离。
      特征空间越紧凑，feature_mahalanobis的TPR天然越高。
      FOAC-AIFP的RPL也利用了类标签做互反点，但RPL是分类器级别的，
      SupCon是特征级别的，更根本。

当前代码位置: ContrastivePretrainer (约L600-850)
当前使用 NT-Xent (SimCLR) loss:
  sim_matrix = F.cosine_similarity(z1.unsqueeze(1), z2.unsqueeze(0), dim=2)
  loss = nt_xent_loss(sim_matrix, temperature)

更改方向:

Step 1: 修改ContrastivePretrainer.train() (约L700-830):

  # 当前: 完全无监督，batch内所有样本互为负例
  # 改为: 有监督对比学习，利用base_classes标签

  # 新增参数:
  supervised_contrastive = True  # True=SupCon, False=SimCLR(回退)

  # 构造batch时同时返回标签:
  # _get_batch() 已返回 (aug1, aug2, labels)
  # labels 只对 base_classes 有效，unknown 标记为 -1

  # SupCon loss 实现:
  def supcon_loss(features, labels, temperature=0.1, mask_unknown=True):
      """
      features: (2*B, D) - 两个augmentation拼接
      labels: (B,) - 类别标签，-1表示不参与
      temperature: 温度参数
      """
      device = features.device
      batch_size = labels.shape[0]

      # 拼接两个view的标签
      labels = labels.repeat(2)  # (2B,)
      if mask_unknown:
          valid_mask = labels >= 0  # 排除unknown类(-1)
      else:
          valid_mask = torch.ones(2 * batch_size, dtype=torch.bool, device=device)

      # 同类mask: label_i == label_j 且 i≠j 且 都valid
      label_mask = labels.unsqueeze(0) == labels.unsqueeze(1)  # (2B, 2B)
      label_mask = label_mask & valid_mask.unsqueeze(1) & valid_mask.unsqueeze(0)
      diag_mask = ~torch.eye(2 * batch_size, dtype=torch.bool, device=device)
      label_mask = label_mask & diag_mask  # 排除自身

      # 负例mask: 都valid 且 不是同类
      neg_mask = ~label_mask & diag_mask & valid_mask.unsqueeze(1)

      # 计算相似度
      sim = F.cosine_similarity(features.unsqueeze(1), features.unsqueeze(0), dim=2) / temperature

      # 对每个anchor，如果有同类正例，计算SupCon loss
      has_positive = label_mask.sum(dim=1) > 0
      if has_positive.sum() == 0:
          # fallback to SimCLR if no pairs
          return nt_xent_loss(features[:batch_size], features[batch_size:], temperature)

      # log-sum-exp trick
      exp_sim = torch.exp(sim) * diag_mask
      log_prob = sim - torch.log(exp_sim.sum(dim=1, keepdim=True) + 1e-8)

      # 只对有正例的anchor计算loss
      mean_log_prob = (log_prob * label_mask.float()).sum(dim=1) / (label_mask.float().sum(dim=1) + 1e-8)

      loss = -mean_log_prob[has_positive].mean()
      return loss

  # 训练循环修改:
  # Phase 0a前半(150 epochs): SimCLR (无监督，所有类参与，学习通用特征)
  # Phase 0a后半(150 epochs): SupCon (base_classes标签，学习判别性特征)
  # 这样既保留SimCLR的泛化性，又获得SupCon的判别力

  if epoch <= 150:
      loss = nt_xent_loss(z1, z2, temperature)
  else:
      features = torch.cat([z1, z2], dim=0)
      loss = supcon_loss(features, batch_labels, temperature)

Step 2: ContrastivePretrainer需要接受标签:
  # _get_batch() 返回 (aug1, aug2, labels)
  # 已有代码只返回 (aug1, aug2)，需修改dataset或collate逻辑
  # TAUDataset已有get_label()方法

────────────────────────────────────────
1.2 可学习OSR阈值 (LearnableThreshold)
────────────────────────────────────────

原理: 当前OSRCalibrator.calibrate()用固定percentile设阈值，
      calib集上表现好但test集下降严重(已观察到此问题)。
      将阈值作为可学习参数，在episodic训练中优化。
      FOAC-AIFP没有这个问题因为它用(N+1)-way直接分类，无需阈值。
      我们的优势: 阈值方法更灵活，可以后处理调整。

代码位置: 新增类 + 修改EpisodicTrainer.train_episode

新增类 (在EpisodicFlowClassifier之后):

  class LearnableOSRThreshold(nn.Module):
      """可学习的OSR阈值，替代固定percentile"""
      def __init__(self, num_scores: int = 1, init_value: float = 0.0):
          super().__init__()
          self.thresholds = nn.Parameter(torch.full((num_scores,), init_value))
          self.temperature = nn.Parameter(torch.tensor(1.0))

      def forward(self, scores: torch.Tensor) -> torch.Tensor:
          """将连续分数转为known/unknown概率"""
          return torch.sigmoid((scores - self.thresholds) * self.temperature.abs())

      def get_threshold(self) -> float:
          return self.thresholds.item()

在EpisodicFlowClassifier中添加:
  self.osr_threshold = LearnableOSRThreshold(num_scores=1, init_value=0.0)

在EpisodicTrainer.train_episode中添加:
  with torch.no_grad():
      known_scores = compute_mahalanobis(query_feats, prototypes)
      if ood_feats is not None:
          unknown_scores = compute_mahalanobis(ood_feats, prototypes)

  known_probs = self.flow_classifier.osr_threshold(known_scores)
  unknown_probs = self.flow_classifier.osr_threshold(unknown_scores)
  L_threshold = -torch.log(known_probs + 1e-8).mean()
  L_threshold += -torch.log(1 - unknown_probs + 1e-8).mean()

  loss += 0.1 * L_threshold

在OSRCalibrator.evaluate_osr中使用学习到的阈值:
  learned_threshold = self.flow_classifier.osr_threshold.get_threshold()


════════════════════════════════════════════════════════════════════
二、osr17b: 互反点学习 + 3级Curriculum OOD (优先级3+4)
════════════════════════════════════════════════════════════════════

前提: osr17a完成，特征质量已有提升
预期收益: 互反点增强feat_mahal TPR 5-10%; Curriculum OOD增强ood_head
工作量: 中
预计耗时: ~3h (仅Phase 2重训)

────────────────────────────────────────
2.1 互反点学习 (Reciprocal Point, 来自ARPL)
────────────────────────────────────────

原理: FOAC-AIFP的RPL预训练已经用了互反点思想！但它是在ResNet-18 backbone
      的预训练阶段学的，且互反点与backbone耦合。
      我们的方法: 在episodic训练中学习互反点，与YAMNet特征适配。
      优势: 互反点随episode动态适应，更灵活。

在EpisodicFlowClassifier中添加:

  # __init__:
  self.num_max_classes = 20
  self.reciprocal_points = nn.Parameter(
      torch.randn(self.num_max_classes, input_dim) * 0.1
  )

  # 新增方法:
  def compute_reciprocal_loss(self, features, prototypes, labels):
      B, D = features.shape
      N = prototypes.shape[0]
      R = self.reciprocal_points[:N]
      R_assigned = R[labels]

      # 已知样本应远离互反点
      dist_to_reciprocal = ((features - R_assigned) ** 2).sum(dim=1)
      L_push = -torch.log(dist_to_reciprocal + 1.0).mean()

      # 互反点多样化正则
      proto_R_sim = F.cosine_similarity(
          prototypes.unsqueeze(1), R.unsqueeze(0), dim=2)
      L_diverse = -proto_R_sim.mean()

      return L_push + 0.1 * L_diverse

  # 新增OSR评分方法:
  def score_reciprocal(self, features, prototypes):
      N = prototypes.shape[0]
      R = self.reciprocal_points[:N]
      dist_proto = torch.cdist(features, prototypes, p=2).min(dim=1).values
      dist_R = torch.cdist(features, R, p=2).min(dim=1).values
      return -dist_proto + dist_R  # known高, unknown低

在EpisodicTrainer.train_episode中加入:
  L_reciprocal = self.flow_classifier.compute_reciprocal_loss(
      query_feats, prototypes, query_labels)
  loss += 0.05 * L_reciprocal

在OSRCalibrator中新增 reciprocal 评分方法。

────────────────────────────────────────
2.2 3级Curriculum OOD (来自CROCO/PEELER)
────────────────────────────────────────

原理: FOAC-AIFP的NPM模块通过交叉注意力从基类权重生成"假类中心"作为
      (N+1)-way的未知类原型。这是一种全局策略，不随训练进度变化。
      我们的方法: 3级递进难度的伪未知，像课程学习一样逐步提升。
      优势: 训练早期用简单OOD稳定分类器，后期用对抗OOD增强鲁棒性。

在EpisodicTrainer中替换现有OOD生成逻辑:

  def generate_curriculum_ood(self, prototypes, features, episode_num, max_episodes):
      progress = min(episode_num / max_episodes, 1.0)

      if progress < 0.33:
          # Level 1: Easy — 原型间插值 + 噪声
          return self._sample_boundary_ood(prototypes, num_samples=15)

      elif progress < 0.66:
          # Level 2: Medium — Hard Negative组合
          N = prototypes.shape[0]
          num_samples = 15
          ood_feats = []
          for _ in range(num_samples):
              n_mix = np.random.randint(2, min(N, 4))
              chosen = np.random.choice(N, n_mix, replace=False)
              weights = np.random.dirichlet(np.ones(n_mix) * 0.5)
              mixed = sum(w * prototypes[c] for w, c in zip(weights, chosen))
              mixed += torch.randn_like(mixed) * 0.1
              ood_feats.append(mixed)
          return torch.stack(ood_feats)

      else:
          # Level 3: Hard — 对抗性生成
          num_samples = 15
          ood_feats = []
          for _ in range(num_samples):
              x = prototypes.mean(dim=0) + torch.randn(prototypes.shape[1], device=self.device) * 0.5
              x = x.clone().detach().requires_grad_(True)
              optimizer_adv = torch.optim.SGD([x], lr=0.05)
              for _ in range(5):
                  log_probs = self.flow_classifier.classify(x.unsqueeze(0), prototypes)
                  entropy = -(log_probs.exp() * log_probs).sum()
                  (-entropy).backward()
                  optimizer_adv.step()
                  optimizer_adv.zero_grad()
              ood_feats.append(x.detach())
          return torch.stack(ood_feats)

  def _sample_boundary_ood(self, prototypes, num_samples=15):
      N = prototypes.shape[0]
      ood_feats = []
      for _ in range(num_samples):
          i, j = np.random.choice(N, 2, replace=False)
          alpha = np.random.beta(0.5, 0.5)
          mixed = alpha * prototypes[i] + (1 - alpha) * prototypes[j]
          mixed += torch.randn_like(mixed) * 0.2
          ood_feats.append(mixed)
      return torch.stack(ood_feats)


════════════════════════════════════════════════════════════════════
三、osr17c: 原型不确定性建模 (优先级5)
════════════════════════════════════════════════════════════════════

前提: osr17b完成
预期收益: K=1~3 shot时收益大，K=5时也有边际收益
工作量: 中
预计耗时: ~2h

原理: FOAC-AIFP的PAM模块通过余弦相似度加权聚合原型，本质上是一种
      基于样本质量的加权平均。但它没有显式建模不确定性。
      我们的方法: 输出分布参数(均值+标准差)，用精度加权的马氏距离。

在EpisodicFlowClassifier.classify()中修改原型计算:

  # 当前:
  prototype = support_feats[mask].mean(dim=0)

  # 改为分布原型:
  proto_mean = support_feats[mask].mean(dim=0)
  if support_feats[mask].shape[0] > 1:
      proto_std = support_feats[mask].std(dim=0).clamp(min=0.01)
  else:
      proto_std = torch.ones_like(proto_mean) * 0.1  # K=1时给较大不确定性

  # 精度加权距离:
  proto_precision = 1.0 / (proto_std ** 2)
  diff = query_feats - proto_mean
  weighted_dist = (diff ** 2 * proto_precision).sum(dim=1)

注意: 这会改变classify()的接口，需同步修改EpisodicTrainer和OSRCalibrator。


════════════════════════════════════════════════════════════════════
四、代码更改汇总 (按文件和区域)
════════════════════════════════════════════════════════════════════

episodic_trainer.py:
  ┌──────────────────┬────────────────┬─────────────────────────────────────┐
  │ 区域              │ 行号约         │ 更改内容                            │
  ├──────────────────┼────────────────┼─────────────────────────────────────┤
  │ ContrastivePre-  │ L600-850       │ osr17a: SupCon loss (后半训练)      │
  │ trainer.train()  │                │ batch返回标签; epoch>150时用SupCon   │
  ├──────────────────┼────────────────┼─────────────────────────────────────┤
  │ EpisodicFlow-    │ L1128-1270     │ osr17a: +LearnableOSRThreshold      │
  │ Classifier       │                │ osr17b: +reciprocal_points参数      │
  │                  │                │        +compute_reciprocal_loss()    │
  │                  │                │        +score_reciprocal()           │
  │                  │                │ osr17c: 分布原型proto_std计算        │
  ├──────────────────┼────────────────┼─────────────────────────────────────┤
  │ EpisodicTrainer  │ L1910-2130     │ osr17a: 阈值loss加入train_episode   │
  │                  │                │ osr17b: curriculum OOD替换现有逻辑   │
  │                  │                │        generate_curriculum_ood()     │
  │                  │                │        _sample_boundary_ood()        │
  ├──────────────────┼────────────────┼─────────────────────────────────────┤
  │ EpisodicTrainer  │ L2153-2224     │ osr17b: reciprocal loss加入主loss   │
  │ train_episode()  │                │ osr17a: threshold loss加入主loss     │
  ├──────────────────┼────────────────┼─────────────────────────────────────┤
  │ OSRCalibrator    │ L2432-3446     │ osr17a: 评估时用可学习阈值          │
  │                  │                │ osr17b: 新增reciprocal评分方法       │
  ├──────────────────┼────────────────┼─────────────────────────────────────┤
  │ main()           │ L3597-3879     │ experiment_dir改为osr17a/17b/17c     │
  │                  │                │ 配置开关: 各启发特性的启用/禁用       │
  │                  │                │ Phase 3: 新增评分方法的评估          │
  └──────────────────┴────────────────┴─────────────────────────────────────┘

其他文件:
  - Reversable_Function/ (NSF模块): 不需要改动
  - utils/TAU22.py (数据集): 不需要改动
  - Phase 0b (base class finetune): 不需要改动


════════════════════════════════════════════════════════════════════
五、实验时间表
════════════════════════════════════════════════════════════════════

Step 0: Baseline — FOAC-AIFP测试
  已有预训练模型，只需运行测试
  耗时: ~30min

  □ 运行 FOAC-AIFP 测试获取 ACC / AUROC / F-score
  □ 记录结果作为论文对比基准

Step 1: osr17a — SupCon + 可学习阈值
  改动: ~80行
  Phase 0a (SupCon): 300 epochs (~1.5h)
  Phase 0b: 20 epochs (~10min)
  Phase 1: 特征提取 (~10min)
  Phase 2: 6000 episodes (~1.5h)
  Phase 3: 评估 (~15min)
  总计: ~4h

  判断标准:
    Base 5-shot > 63% → 特征质量提升，进入osr17b
    Base 5-shot < 60% → SupCon后半训练有问题，检查标签传递
    feat_mahal TPR > 14% → SupCon有效
    可学习阈值的calib→test TPR衰减 < 3% → 阈值学习有效

Step 2: osr17b — 互反点 + Curriculum OOD
  改动: ~100行
  Phase 2重训: 6000 episodes (~1.5h, 复用osr17a的Phase 0)
  总计: ~2h

  判断标准:
    reciprocal TPR > 10% → 互反点有OSR信号
    reciprocal + feat_mahal 融合 TPR > osr17a feat_mahal + 5% → 融合有效
    ood_head TPR > 13% → curriculum OOD有效
    Base 5-shot不退化 (> 62%)

Step 3: osr17c — 原型不确定性
  改动: ~60行
  Phase 2重训: 6000 episodes (~1.5h, 复用osr17b的Phase 0)
  总计: ~2h

  判断标准:
    1-shot Base > 55% → 不确定性建模有效
    5-shot Base不退化


════════════════════════════════════════════════════════════════════
六、关键检查点
════════════════════════════════════════════════════════════════════

Baseline (FOAC-AIFP) 检查点:
  □ 测试正常运行，输出 ACC / AUROC / F-score
  □ AUROC > 0.6 (论文报告的基准水平)
  □ ACC > 50% (2-way 5-shot)

osr17a 检查点:
  □ Phase 0a: epoch 150切换SimCLR→SupCon时loss不应跳变
  □ Phase 0a: SupCon阶段类内距离减小
  □ t-SNE: base类簇紧凑、类间分离
  □ Phase 2: 可学习阈值在前500个episode内快速收敛
  □ Phase 3: feat_mahal TPR @ 95% TNR
  □ Phase 3: 可学习阈值在test集上的TPR衰减 (对比percentile方法)

osr17b 检查点:
  □ reciprocal_points在前1000 episode内学到有意义的位置
  □ reciprocal score: known高分 vs unknown低分的separation > 0.3
  □ Curriculum OOD: Level 1→2→3切换时ood_head loss不跳变
  □ Level 3 对抗样本确实让分类器不确定 (entropy > 1.3 for 5-way)
  □ ood_head TPR对比osr17a有提升

osr17c 检查点:
  □ proto_std分布合理: K=1时~0.1, K=5时~0.03, K=10时~0.01
  □ 1-shot分类精度提升
  □ feat_mahal使用类条件精度后TPR变化


════════════════════════════════════════════════════════════════════
七、osr15/osr16失败的教训 (设计osr17时必须避免)
════════════════════════════════════════════════════════════════════

1. 不要联合训练flow+分类器 (osr15教训)
   osr17: 不使用flow作为主OSR方法，用feature_mahalanobis+互反点

2. 特征质量是OSR上限 (osr13→osr15的经验)
   → osr17优先投入SupCon特征升级，而非继续优化OSR head

3. 阈值不能只靠校准集percentile (calib→test下降)
   → osr17使用可学习阈值，在训练中优化

4. 伪未知样本质量决定ood_head上限
   → osr17使用3级curriculum OOD，而非简单mixup

5. 简单方法优先
   osr13的z_consistency 20.22% 已经是最好的结果
   复杂的flow方法(osr15)反而崩溃到3.74%
   → osr17优先改特征(SupCon)和阈值(可学习)，这些改动风险最小


════════════════════════════════════════════════════════════════════
八、与论文写作的衔接
════════════════════════════════════════════════════════════════════

论文对比实验表格 (预期):

  ┌───────────────────────┬──────────┬─────────┬─────────┬─────────┐
  │ 方法                   │ Backbone │ ACC     │ AUROC   │ F-score │
  ├───────────────────────┼──────────┼─────────┼─────────┼─────────┤
  │ FOAC-AIFP (baseline)  │ ResNet-18│ (填入)  │ (填入)  │ (填入)  │
  │ FOAC-AIFP (baseline)  │ ResNet-18│ (填入)  │ (填入)  │ (填入)  │
  │                       │          │         │         │         │
  │ Ours (osr17a SupCon)  │ YAMNet   │ >63%    │ >0.65   │ >0.45   │
  │ Ours (osr17b +Rcp+OOD)│ YAMNet   │ >63%    │ >0.70   │ >0.50   │
  │ Ours (osr17c +Unc)    │ YAMNet   │ >64%    │ >0.72   │ >0.52   │
  └───────────────────────┴──────────┴─────────┴─────────┴─────────┘

  注意: 需在我们的评估流程中也计算AUROC和F-score以对齐FOAC-AIFP指标。
  当前我们主要用 TPR@95%TNR，论文需要补全AUROC。

方法创新点写作框架:
  1. SupCon有监督对比特征学习 (vs FOAC-AIFP的RPL+GCPL无监督预训练)
  2. Episodic互反点学习 (vs FOAC-AIFP的预训练阶段固定互反点)
  3. Curriculum OOD渐进训练 (vs FOAC-AIFP的NPM全局假类中心)
  4. 端到端可学习OSR阈值 (vs FOAC-AIFP的(N+1)-way直接分类)
  5. 原型不确定性建模 (vs FOAC-AIFP的PAM确定性加权)

消融实验表格:
  ┌──────────────┬─────────┬──────────┬──────────┬──────────┐
  │ 配置          │ Base 5s │ feat_mah │ reciproc │ ood_head │
  ├──────────────┼─────────┼──────────┼──────────┼──────────┤
  │ osr17a(SupC) │ >63%    │ >14%     │ —        │ >12%     │
  │ osr17b(+Rcp) │ >63%    │ >14%     │ >10%     │ >15%     │
  │ osr17c(+Unc) │ >64%    │ >16%     │ >12%     │ >16%     │
  └──────────────┴─────────┴──────────┴──────────┴──────────┘
  TPR @ 95% TNR; 需同时报告AUROC以便与FOAC-AIFP对比


════════════════════════════════════════════════════════════════════
九、Reciprocal Loss 实现修复记实 (2026-04-26)
════════════════════════════════════════════════════════════════════

背景: osr17b 实现中发现 reciprocal_points 从未被训练，一直保持随机初始化状态

问题排查过程:

1. 初步症状
   - best checkpoint (ep 150) 的 reciprocal_points std=0.0995 ≈ N(0, 0.1²) 初始值
   - final checkpoint (ep 2250) 的 reciprocal_points 完全相同
   - 训练日志显示 scale_struct 在 ep 600+ 激活，但参数未更新

2. 根因分析

   2.1 优化器不包含该参数
   - old checkpoint 的 optimizer 只有 53 个参数
   - reciprocal_points [20, 64] 不在 optimizer 中
   - 原因: 代码更新后 resume 加载了旧的 optimizer state

   2.2 损失函数设计缺陷
   - 原始损失: L = -log(dist + 1) + 0.1 * cos(P, R)
   - 问题: dist ≈ 64 时，log(65) ≈ 4.17，梯度 ≈ 1/(64+1) * 2*64 ≈ 2，太小
   - 更严重: 改为 margin loss 后，realistic dist ~2000，ReLU(50-2000)=0，无梯度

   2.3 损失权重太小
   - 原始: 0.05 * scale_struct
   - ep 301 时 scale_struct=0.003，有效权重 = 0.00015
   - 即使有梯度，更新步长也微乎其微

   2.4 梯度裁剪限制
   - 全局 gradient clipping: max_norm=0.5
   - reciprocal_points 梯度 norm ~4-36，被裁剪到 0.5
   - 削弱了 ~90% 的更新幅度

3. 修复方案

   3.1 优化器兼容性修复 (episodic_trainer.py:2443-2454)
   ```python
   # Resume from checkpoint 时添加缺失参数
   current_param_ids = set(id(p) for p in self.flow_classifier.parameters())
   optim_param_ids = set()
   for group in self.optimizer.param_groups:
       for p in group['params']:
           optim_param_ids.add(id(p))
   missing_params = [p for p in self.flow_classifier.parameters()
                    if id(p) not in optim_param_ids]
   if missing_params:
       print(f"  [FIX] Adding {len(missing_params)} new parameters to optimizer")
       self.optimizer.add_param_group({'params': missing_params})
   ```

   3.2 损失函数重设计 (episodic_trainer.py:1650-1661)
   - 放弃: 伪未知样本推离 (在 64-dim 特征空间难以定义合理 margin)
   - 采用: 原型吸引损失
   ```python
   def compute_reciprocal_loss(self, features, prototypes, labels):
       N = prototypes.shape[0]
       R = self.reciprocal_points[:N]
       # 移动 R 靠近原型 (间接推离特征)
       dist_R_to_proto = ((R - prototypes) ** 2).sum(dim=1)
       return dist_R_to_proto.mean()
   ```
   - 效果: loss ~1645，梯度 norm ~36，强信号

   3.3 权重提升 (episodic_trainer.py:2350)
   - 从 0.05 → 5.0 (100x)
   - ep 301 时有效贡献 = 5.0 * 0.003 * 17.59 ≈ 0.22 (显著)

   3.4 梯度裁剪豁免 (episodic_trainer.py:2512-2518)
   ```python
   params_to_clip = [p for p in self.flow_classifier.parameters()
                    if p is not self.flow_classifier.reciprocal_points]
   if params_to_clip:
       torch.nn.utils.clip_grad_norm_(params_to_clip, 0.5)
   ```

4. 验证结果

   - 测试配置: 500 episodes, Phase 3 从 ep 301 开始
   - 诊断输出 (ep 301):
     * L_reciprocal = 17.59
     * weighted contribution = 0.22
     * Parameter [0,0] changed: 0.000050
     * Mean absolute change: 0.001348
   - ✅ reciprocal_points 成功被训练

5. 重要启发

   5.1 特征空间的 scale 决定损失函数设计
   - 64-dim YAMNet 特征: 典型距离 ~2000
   - 512-dim ResNet 特征: 典型距离可能更大
   - 固定 margin (如 50) 完全失效
   - 必须: 使用相对尺度 (如除以特征 norm) 或原型吸引

   5.2 梯度流检查清单
   1. 参数是否在 optimizer 中? (optimizer.param_groups)
   2. 梯度是否存在且非零? (p.grad is not None and p.grad.abs().sum() > 0)
   3. 梯度是否被裁剪? (检查 clip_grad_norm_ 调用)
   4. 学习率是否合理? (lr * grad 的量级应 > 1e-6 才有可见更新)
   5. 损失权重是否足够大? (总损失中占比 > 1%)

   5.3 checkpoint resume 的陷阱
   - state_dict 加载不会自动更新 optimizer
   - 新增参数必须手动 add_param_group
   - 建议: 从头训练或保存完整 checkpoint (含 optimizer)

   5.4 诊断日志的重要性
   - 第一次激活时打印: scale_struct, loss 值, weighted contribution
   - 检查关键参数的更新: 记录 optimizer.step() 前后的值
   - 对比预期更新 vs 实际更新 (lr * grad)

   5.5 互反点重新理解
   - 原始论文: 推离已知样本 (难以定义合理 margin)
   - 实用方案: 靠近原型 (简单有效)
   - OSR 评分: score = -dist(prototype) + dist(reciprocal)
   - 如果 reciprocal 靠近 prototype，known 样本得高分，unknown 得低分
   - 这与原意图等价: reciprocal 是"反向原型"，但定义在特征空间而非反向空间

6. 对 osr17b 实施的影响

   - ✅ reciprocal loss 现已可工作
   - ⚠️ 但需要更长训练才能看到 OSR 性能提升
   - 建议: 增加 episodes 到 6000，确保 Phase 3 充分训练
   - 评估: 在 Phase 3 评估中添加 'reciprocal' 方法

7. 代码改动位置

   episodic_trainer.py:
   - L2443-2454: 优化器兼容性修复
   - L1650-1661: 损失函数重设计
   - L2350: 权重调整 (0.05 → 5.0)
   - L2512-2518: 梯度裁剪豁免



════════════════════════════════════════════════════════════════════
十、osr17b 训练结果分析 (2026-04-26)
════════════════════════════════════════════════════════════════════

训练配置:
- Experiment: yamnet_fewshot_osr17b
- 训练episodes: 3300 (early stop at ep 3300)
- Best Val Acc: 55.93% (ep 3150)
- Phase 3: 2700 episodes (ep 601-3300)
- Patience: 2000 episodes

────────────────────────────────────────
Base类分类性能 (5-way)
────────────────────────────────────────

| 配置 | 实际 | 目标 | 状态 |
|------|------|------|------|
| 1-shot | 50.75% | >55% | ⚠️ 未达标 |
| 5-shot | 58.08% | >63% | ❌ 未达标 |
| 10-shot | 59.33% | >65% | ⚠️ 未达标 |

**诊断**: Base分类性能不足可能是Phase 0预训练质量的问题。

────────────────────────────────────────
OSR性能 (TPR@95%TNR, 5-way 5-shot)
────────────────────────────────────────

| 方法 | 实际 | 目标 | 状态 |
|------|------|------|------|
| feature_mahalanobis | 11.46% | >14% | ⚠️ 未达标 |
| reciprocal | 9.19% | >10% | ❌ 未达标 |
| ood_head | 9.78% | >15% | ❌ 未达标 |
| flow_hybrid | 4.59% | - | 弱 |
| flow_energy | 2.85% | - | 弱 |
| z_consistency | 4.88% | - | 弱 |
| z_gap | 3.34% | - | 弱 |
| z_mahal_fusion | 2.14% | - | 弱 |

────────────────────────────────────────
问题诊断
────────────────────────────────────────

1. Base分类不足 (58.08% vs 63%)
   - 可能原因: Phase 0预训练被旧checkpoint覆盖
   - 验证方法: 检查base_feature_extractor.pth的FC层std
   - 如果std≈0.01 (初始化值)，说明SupCon未生效

2. Reciprocal TPR低 (9.19% vs 10%)
   - reciprocal_points已被训练 (std: 0.0925)
   - Phase 3训练时长可能不够 (2700 episodes)
   - 建议: 延长训练或增加权重

3. OOD Head TPR低 (9.78% vs 15%)
   - Curriculum OOD切换太快
   - Level 3仅700 episodes (ep 2601-3300)
   - 建议: 延长Phase 3训练

4. Flow方法弱 (2-5% TPR)
   - z_norm²偏离目标 (40-70 vs 32)
   - 可能是特征空间scale问题

────────────────────────────────────────
实施的修改 (2026-04-26)
────────────────────────────────────────

修改1: 增加patience (L2345)
- patience_episodes: 2000 → 3000
- 预期: 允许Phase 3更充分训练

修改2: 延长Phase 3 ramp (L2265)
- stage3_len: warmup * 4 → warmup * 6
- 预期: 1200 episodes完成structural loss ramping

修改3: 增加reciprocal权重 (L2515)
- reciprocal weight: 5.0 → 10.0
- 预期: 强化OSR信号

修改4: 降低num_episodes (L3993)
- num_episodes: 10000 → 6000
- 预期: patience会提前停止，设置合理上限

────────────────────────────────────────
预期改进
────────────────────────────────────────

| 指标 | 当前 | 修改后 |
|------|------|--------|
| Base 5s | 58.08% | 60-61% |
| reciprocal | 9.19% | 11-13% |
| ood_head | 9.78% | 12-14% |
| feat_mahal | 11.46% | 13-15% |
| AUROC | ~0.55 | ~0.60 |

────────────────────────────────────────
Flow组件保留状态
────────────────────────────────────────

保留的flow组件:
1. Conditional cINN (4层coupling) - 用于z_*方法
2. Background flow - 用于flow_energy/flow_hybrid
3. Feature adaptation (feature_adapter)
4. Projection head (projection)

这些组件在评估中正常运行，虽然性能较弱但保留了OSR多样性。


════════════════════════════════════════════════════════════════════
十一、osr17c 训练结果分析 + osr17d 改进方案 (2026-04-26)
════════════════════════════════════════════════════════════════════

训练配置 (osr17c):
- Patience: 3000, Stage3: 1200, num_episodes: 6000
- Reciprocal权重: 10.0 (从osr17b的5.0增加)

────────────────────────────────────────
osr17c vs osr17b 对比
────────────────────────────────────────

| 指标 | osr17b | osr17c | 变化 |
|------|--------|--------|------|
| Best Val Acc | 55.93% | 59.24% | +3.31% |
| Base 5-shot | 58.08% | 59.20% | +1.12% |
| feat_mahal TPR | 11.46% | 11.46% | 0 |
| reciprocal TPR | 9.19% | 9.34% | +0.15 |
| ood_head TPR | 9.78% | 9.93% | +0.15 |
| flow_hybrid TPR | 4.59% | 3.47% | **-1.12** |
| flow_energy TPR | 2.85% | 1.87% | **-0.98** |
| z_consistency | 2.14% | 1.57% | **-0.57** |

分类性能提升但**flow方法全面退化**。

────────────────────────────────────────
根因分析
────────────────────────────────────────

1. 特征空间类重叠严重 (数据异常根因)
   - 39.4%训练样本的最近原型不是自己的类
   - Nearest-prototype accuracy: 60.6%
   - Class 0: 42.6%, Class 5: 37.1% (最差)
   - Inter/intra距离比: 8.4/4.5 = 1.87 (理想>3)
   - 各维度std不一致: 0.83-1.00 → 距离几何各向异性

2. loss竞争失衡
   - reciprocal权重10.0 → L_reciprocal贡献~2100
   - L_gaussian贡献~0.5 → flow正则化被完全压制
   - Total loss ~420 (osr17b ~210)

3. z_norm²发散无法纠正
   - 自适应惩罚阈值 2*32=64 过高
   - 实际z_norm² 52-63时不触发惩罚
   - 触发时权重仅0.025，远不够拉回

4. condition_network禁用
   - flow用原始投影原型做条件
   - 缺乏学习变换，限制条件密度估计能力

────────────────────────────────────────
osr17d 实施方案 (5项修改)
────────────────────────────────────────

修改1: 特征per-dim标准化 (L1017-1088 + L4093-4103)
- CACHE_VERSION bump: v19 → v20
- extract_and_cache增加norm_mean/norm_std参数
- 训练集自动计算统计量，应用到calib/test
- 预期: 类重叠从39.4%降至~30%

修改2: reciprocal权重回调 (L2534)
- 10.0 → 6.0 (平衡flow loss)
- 预期: L_reciprocal贡献从~2100降至~800-1200

修改3: 启用condition_network (L4140)
- use_condition_network=True
- 预期: flow条件信号更丰富，密度估计更准确

修改4: z_norm²连续软惩罚 (L2536-2548)
- 触发阈值: 2.0×target → 1.5×target (=48)
- 系数: 0.2 → 0.3, cap: 1.0 → 2.0
- 预期: z_norm²从52-68降至35-45

修改5: experiment_dir保持yamnet_fewshot_osr17

────────────────────────────────────────
预期改进 (osr17d)
────────────────────────────────────────

| 指标 | osr17c | osr17d预期 |
|------|--------|-----------|
| Base 5-shot | 59.20% | 62-65% |
| feat_mahal | 11.46% | 13-16% |
| reciprocal | 9.34% | 11-13% |
| flow_hybrid | 3.47% | 6-9% |
| ood_head | 9.93% | 12-15% |
| z_norm² | 52-68 | 35-45 |


════════════════════════════════════════════════════════════════════
十二、osr17d 训练结果 + osr17e 根因修复方案 (2026-04-26)
════════════════════════════════════════════════════════════════════

训练配置 (osr17d):
- Patience: 3000, Stage3: 1200, num_episodes: 6000
- Reciprocal权重: 6.0 (从osr17c的10.0降低)
- Per-dim特征标准化 (CACHE_VERSION v20)
- condition_network: 启用
- z_norm²连续软惩罚: 阈值1.5×target=48, 系数0.3, cap 2.0

────────────────────────
osr17d 训练过程 (ep 2250/6000, 仍在运行)
────────────────────────

| Episode | CE     | Total   | Acc    | Val Acc | scale_struct | z_norm² | z_std |
|---------|--------|---------|--------|---------|--------------|---------|-------|
| 150     | 2.087  | 2.097   | 60.95% | 58.07%  | 0.0          | 44.6    | 0.652 |
| 750     | 1.219  | 24.846  | 57.93% | 59.27%  | 0.1          | 145.3   | 0.502 |
| 1350    | 1.213  | 188.545 | 59.39% | 59.98%  | 0.6          | 201.2   | 0.486 |
| 1950    | 1.180  | 317.099 | 60.42% | 60.44%  | 1.0          | 200.6   | 0.513 |
| 2250    | 1.145  | 313.414 | 60.61% | 60.38%  | 1.0          | 188.1   | 0.502 |

关键观察:
- z_norm²从ep 600开始发散: 44.6→145→201→200→188
- Total loss从ep 750开始爆炸: 2→25→189→317→313
- CE loss正常下降: 2.09→1.21→1.18→1.15
- Val Acc有提升: 58.07%→60.44%(best@ep1950)

────────────────────────
根因分析
────────────────────────

1. z_norm²发散的根本原因: detached tensor BUG

   L2536-2548的z_norm²软惩罚使用了 info['L_gaussian']，
   而info中存储的是 L_gaussian.detach() (L1964)。
   因此 loss += adaptive_w * detached_tensor 产生零梯度！
   惩罚完全无效，z空间只有扩展力没有收缩力。

   扩展力 (latent_repel + non_gaussian + z_contrastive) = 0.2
   收缩力 (gaussian prior) = 0.1
   无效力 (z_norm² penalty, detached) = 0
   → z_norm²自由发散至200

2. Total loss爆炸: reciprocal loss量级失衡

   compute_reciprocal_loss在64维空间做L2²距离:
   dist_R_to_proto = ((R - prototypes)**2).sum(dim=1).mean() ≈ 50
   贡献 = 6.0 × 1.0 × 50 = 300 (占Total的96%)

   虽然梯度不流向flow，但导致Total失真、训练动态不稳定。

3. Flow OSR方法失效的根因: z_norm²发散

   z_norm²=200时(目标32)，flow的密度估计完全失真:
   - 正确类的z值模远大于N(0,I)
   - log p(z) ≈ -200/2 + const → 所有样本的flow密度都很低
   - flow_hybrid, flow_energy, z_consistency等方法无法区分known/unknown

   这不是架构问题，是训练BUG的后果。

4. norm stats重算BUG (L4111-4112)

   从已标准化的train_cache.features重算mean/std → ≈0/≈1
   正确做法: 从缓存文件加载原始norm_mean/norm_std
   当前运行未受影响(三个split都命中缓存)，但新提取时会出错。

────────────────────────
osr17e 实施方案 (5项修复)
────────────────────────

修改1: z_norm² + z_std直接正则化 (替换L2536-2548)

  问题: osr17d的软惩罚用detached tensor → 零梯度
  修复: 直接从z_all计算，保持梯度流

  ```python
  if z_all is not None:
      target_norm = self.criterion.feature_dim  # 32
      z_correct = z_all[torch.arange(B, device=device), query_labels]
      z_norm_sq = (z_correct ** 2).sum(dim=1).mean()
      z_std_per_dim = z_correct.std(dim=0).mean()

      # 二次惩罚: z_norm² → target
      overshoot = z_norm_sq / target_norm - 1.0
      z_norm_penalty = 0.3 * overshoot ** 2

      # z_std惩罚: 防止维度坍缩
      z_std_penalty = 0.5 * (z_std_per_dim - 1.0) ** 2

      loss = loss + z_norm_penalty + z_std_penalty
  ```

  预期: z_norm²从200收敛至32-45, z_std从0.5收敛至0.8-1.0

修改2: reciprocal loss per-dim归一化 (L1679)

  问题: 64维L2²距离≈50，权重6.0贡献300
  修复: 除以input_dim (64)

  ```python
  L = dist_R_to_proto.mean() / self.input_dim  # ~0.78
  ```

  权重保持6.0，有效贡献 = 6.0 × 0.78 ≈ 4.7 (合理)

修改3: lambda_gaussian增强 (L1736)

  0.1 → 0.3
  配合修改1，提供更强的基线gaussian正则化
  扩展力/收缩力比: 0.2/(0.3+0.3) = 0.33 (平衡)

修改4: norm stats从缓存加载 (替换L4112-4114)

  ```python
  train_cache_data = torch.load('experiment/fewshot_cache/train_features.pt')
  train_norm_mean = train_cache_data.get('norm_mean', None)
  train_norm_std = train_cache_data.get('norm_std', None)
  ```

修改5: CACHE_VERSION保持不变 (v20_yamnet_normalized)
  特征提取逻辑不变，无需bump版本。
  删除旧缓存强制重训: 删除fewshot_best.pth即可触发Phase 2重训。

────────────────────────
Flow OSR方法恢复策略
────────────────────────

osr17e修复z_norm²后，flow密度估计应恢复正常。
预期Flow方法TPR改善:

| 方法         | osr17d (z=200) | osr17e预期 (z≈32) | 原因 |
|-------------|----------------|-------------------|------|
| flow_hybrid | 3-5%           | 8-14%             | flow密度恢复区分度 |
| flow_energy | 2-3%           | 6-10%             | energy score有意义 |
| z_consistency| 2-5%          | 7-12%             | z空间几何恢复正常 |
| z_mahal_fusion| 2-3%         | 8-13%             | z与mahal可融合 |
| feat_mahal  | 11-12%         | 13-16%            | 基础方法不受z影响 |

Flow OSR方法保留完整:
1. Conditional cINN (4层coupling) → z空间密度估计
2. Background flow → likelihood ratio (flow_hybrid)
3. Energy-based scoring → flow_energy
4. z_consistency → z空间一致性检查
5. z_mahal_fusion → z+mahal融合评分

这些方法在osr13中z_norm²≈32时曾达到20.22% TPR (z_consistency)，
osr17e恢复z_norm²后有望重现或超越。

────────────────────────
预期改进 (osr17e)
────────────────────────

| 指标 | osr17d | osr17e预期 | 原因 |
|------|--------|-----------|------|
| z_norm² | 188-201 | 32-45 | 惩罚BUG修复 |
| z_std | 0.48-0.52 | 0.8-1.0 | z_std正则化 |
| Total loss | 300-320 | 5-15 | reciprocal归一化 |
| Base 5-shot | 60.44% | 62-65% | flow不再干扰分类 |
| feat_mahal | 11-12% | 13-16% | 特征适配器更稳定 |
| flow_hybrid | 3-5% | 8-14% | z空间恢复正常 |
| flow_energy | 2-3% | 6-10% | 密度估计有意义 |
| reciprocal | 9-10% | 11-14% | 更平衡的训练 |
| ood_head | 10-11% | 12-15% | 更好的OOD特征 |

────────────────────────
osr17e 检查点
────────────────────────

□ z_norm²在ep 300前收敛到45以下 (修复验证)
□ z_std在ep 600前收敛到0.7以上 (维度利用)
□ Total loss < 20 (loss平衡)
□ CE loss不退化 (仍 < 1.3)
□ Best Val Acc > 60% (分类不降)
□ Flow方法TPR > 8% (flow恢复)
□ 所有6种OSR评分方法均有信号 (> 5% TPR)

────────────────────────
代码改动位置汇总
────────────────────────

episodic_trainer.py:
  ┌──────────────────┬────────────────┬──────────────────────────────────────┐
  │ 区域              │ 行号约         │ 更改内容                             │
  ├──────────────────┼────────────────┼──────────────────────────────────────┤
  │ EpisodicLoss     │ L1736          │ osr17e: lambda_gaussian 0.1→0.3      │
  │ __init__         │                │                                      │
  ├──────────────────┼────────────────┼──────────────────────────────────────┤
  │ compute_recip-   │ L1669-1682     │ osr17e: L2²除以input_dim归一化       │
  │ rocal_loss       │                │ ~50 → ~0.78                          │
  ├──────────────────┼────────────────┼──────────────────────────────────────┤
  │ train_episode    │ L2536-2552     │ osr17e: 直接z_norm²+z_std正则化      │
  │ z惩罚区域        │                │ 替换detached BUG, 两个二次惩罚       │
  ├──────────────────┼────────────────┼──────────────────────────────────────┤
  │ main() Phase 1   │ L4112-4118     │ osr17e: 从缓存文件加载norm stats     │
  │ norm stats       │                │ 而非从已标准化features重算           │
  └──────────────────┴────────────────┴──────────────────────────────────────┘


════════════════════════════════════════════════════════════════════
十三、osr17e 训练结果 + osr17f Flow OSR增强方案 (2026-04-26)
════════════════════════════════════════════════════════════════════

训练配置 (osr17e):
- 与osr17d相同的Phase 0/1 (复用缓存v20)
- 5项BUG修复: z_norm²惩罚梯度、reciprocal归一化、lambda_gaussian、
  norm stats加载、z_std正则化

────────────────────────
osr17e 训练结果 (完整, Early Stop@ep4650)
────────────────────────

训练过程关键指标:

| Episode | CE    | Total | Acc    | Val Acc | z_norm² | z_std |
|---------|-------|-------|--------|---------|---------|-------|
| 150     | 2.080 | 2.13  | 60.96% | 57.96%  | 35.2    | 0.733 |
| 600     | 1.214 | 1.63  | 57.20% | 53.16%  | 33.9    | 0.928 |
| 1350    | 1.190 | 4.38  | 59.84% | 60.60%  | 33.2    | 0.935 |
| 2250    | 1.126 | 6.26  | 61.45% | 62.00%  | 33.4    | 0.950 |
| 3000    | 1.101 | 6.35  | 61.20% | 66.47%  | 33.4    | 0.957 |
| 4200    | 1.101 | 6.29  | 64.54% | 67.18%  | 33.0    | 0.965 |
| 4500    | 1.075 | 6.21  | 65.76% | 67.38%  | 32.9    | 0.964 |

**对比osr17d:**

| 指标 | osr17d | osr17e | 变化 |
|------|--------|--------|------|
| z_norm² | 188-201 | 33.0-33.9 | **修复!** (目标32) |
| z_std | 0.48-0.52 | 0.93-0.97 | **修复!** (目标1.0) |
| Total loss | 313-317 | 2.1-6.3 | **修复!** |
| Best Val Acc | 60.44% | 67.38% | **+6.94%** |
| Base 5-shot | ~60% | 69.93% | **+9.5%** |
| Base 10-shot | - | 72.31% | 新高 |

分类性能 (5-way):

| 配置 | Accuracy | 目标 | 状态 |
|------|----------|------|------|
| 1-shot | 59.77% | >55% | ✅ |
| 5-shot | 69.93% | >63% | ✅ 达标! |
| 10-shot | 72.31% | >65% | ✅ |

OSR性能 (TPR@95%TNR, 5-way 5-shot):

| 方法 | Calib TPR | Test TPR | Calib→Test Gap |
|------|-----------|----------|----------------|
| feat_mahal | 15.57% | 12.54% | 3.03% |
| reciprocal | 16.24% | 8.72% | **7.52%** |
| ood_head | 18.26% | 10.12% | **8.14%** |
| flow_hybrid | 17.13% | 9.15% | **7.98%** |
| flow_energy | 6.19% | 5.03% | 1.16% |
| z_consistency | 3.50% | 4.22% | -0.72% |
| z_gap | 2.51% | 3.90% | -1.39% |
| z_mahal_fusion | 4.75% | 5.09% | -0.34% |

────────────────────────
osr17e 问题诊断
────────────────────────

1. Z空间方法全部失效 (TPR 3-5%)

   z_consistency分数: known [-0.87,-0.74] mean=-0.82
                      unknown [-0.87,-0.74] mean=-0.80
   分离度仅 -0.01 → 无区分力

   根因: flow + condition_network 过于强大，将任何输入都映射到z≈N(0,I)。
   无论conditioning是正确还是错误的proto，flow都能归一化。
   这是flow的"过度能力"问题 — 对密度估计好，但对OSR不利。

   影响: z_consistency, z_gap, z_mahal_fusion 全部失去OSR信号。

2. Calib→Test泛化差距大

   reciprocal: 16.24%→8.72%, ood_head: 18.26%→10.12%
   这些方法在calib集上学到的阈值/参数不能迁移到test集。

   reciprocal根因: R≈proto (loss使R靠近proto)，导致评分信号
   score = -dist_proto + dist_R ≈ 0 对所有样本。

   ood_head根因: 5维特征(min_dist, dist_ratio, softmax_max, entropy, feat_norm)
   的分布从calib到test发生偏移。

3. Flow OSR方法虽已修复z_norm²但仍弱

   flow_hybrid calib 17.13% → test 9.15%
   使用的是z-space mahal (弱, ~5% TPR) + flow energy (5%)
   融合两弱方法 ≠ 强方法

────────────────────────
osr17f 实施方案 (3项评分改进, 无需重训)
────────────────────────

核心思路: osr17e训练已达标(67.38% val, z_norm²≈32)，
         不需要重训Phase 2。仅改进Phase 3评分策略。

修改1: flow_hybrid改用feat-space mahal + per-round alpha优化

  问题: 当前flow_hybrid = flow_energy + z-space_mahal (两弱方法)
        z-space mahal TPR ≈ 5%, flow_energy TPR ≈ 5%
  修复:
    - score_samples_combined: score_samples_mahalanobis → score_samples_feat_mahalanobis
    - flow_hybrid变为: flow_energy + feat_mahal (12.54% TPR)
    - calibrate/evaluate_osr中的统计量也改用feat_mahal
    - 添加per-round alpha优化 (Youden J), 类似z_mahal_fusion的做法

  预期: flow_hybrid test TPR从9.15%提升至14-16%
        (feat_mahal 12.54% + flow信号的边际增益)

修改2: 新增anti_prototype评分 (几何方法, 无需训练)

  原理: 将proto关于feature mean做几何反射 → anti_proto
        anti_proto = 2 * mean(features) - proto
        known: 靠近proto, 远离anti_proto → score高
        unknown: 远离两者 → score低

  优势: 纯几何，无训练参数，不受calib→test分布偏移影响
  新增: score_anti_prototype() + score_anti_prototype_all() + 评分路由

修改3: 更新Phase 3评估方法列表

  新增: anti_prototype, z_reconstruction
  移除: z_gap (最弱, 与z_consistency重叠)
  最终方法列表:
    feature_mahalanobis, anti_prototype, reciprocal,
    z_consistency, z_reconstruction, z_mahal_fusion,
    flow_hybrid, flow_energy, ood_head

────────────────────────
Flow原理充分利用策略
────────────────────────

osr17f保留并增强的flow组件:

1. Conditional cINN (4层coupling)
   → 用于flow_energy评分 (log p(x|c)密度估计)
   → 贡献于flow_hybrid融合评分

2. Background flow
   → 保留用于likelihood_ratio (如需启用)

3. Feature adaptation (feature_adapter)
   → 所有评分方法的基础, 在训练中学到特征变换

4. Condition network
   → 增强flow的条件信号 (osr17d启用, osr17e验证有效)

Flow不再主导但持续贡献的OSR路径:
  flow_energy → flow_hybrid的一部分 (与feat_mahal融合)
  z_reconstruction → 探索性方法 (已实现, osr17f首次评估)
  z_consistency/z_mahal_fusion → 保留作为基线对比

Flow主导路径 → Flow辅助路径的战略转变:
  osr13/15/17a-d: flow是OSR的核心 (密度估计, z空间分析)
  osr17e: 发现flow"过度归一化"导致z空间方法失效
  osr17f: flow转为辅助角色 (flow_energy增强feat_mahal)

这一转变的核心逻辑:
  特征空间距离 (feat_mahal, anti_prototype) 提供更可靠的OSR信号
  Flow密度提供额外的边际增益 (通过fusion)
  两者互补: 几何距离 + 统计密度

────────────────────────
预期改进 (osr17f vs osr17e)
────────────────────────

| 方法 | osr17e Test TPR | osr17f预期 | 原因 |
|------|-----------------|-----------|------|
| feat_mahal | 12.54% | 12-13% | 不变(基线) |
| anti_prototype | - | 10-14% | 几何方法,稳定 |
| reciprocal | 8.72% | 8-9% | 不变(对比) |
| flow_hybrid | 9.15% | **14-16%** | feat_mahal替代z-mahal |
| flow_energy | 5.03% | 5-6% | 不变 |
| ood_head | 10.12% | 10-11% | 不变 |
| z_reconstruction | - | 6-10% | 新方法,待验证 |
| z_consistency | 4.22% | 4-5% | 不变(flow过度归一化) |
| z_mahal_fusion | 5.09% | 5-6% | 不变 |

预期最佳方法: flow_hybrid (14-16%) 或 anti_prototype (10-14%)

────────────────────────
osr17f 检查点
────────────────────────

□ flow_hybrid使用feat_mahal后test TPR > 13%
□ anti_prototype test TPR > reciprocal (8.72%)
□ z_reconstruction有OSR信号 (TPR > 5%)
□ flow_hybrid的calib→test gap < 5% (从7.98%改善)
□ 所有方法test TPR不低于osr17e

────────────────────────
代码改动位置汇总 (osr17f)
────────────────────────

episodic_trainer.py:
  ┌──────────────────┬────────────────┬──────────────────────────────────────┐
  │ 区域              │ 行号约         │ 更改内容                             │
  ├──────────────────┼────────────────┼──────────────────────────────────────┤
  │ score_samples_   │ L3081-3106     │ osr17f: flow_hybrid改用feat_mahal    │
  │ combined         │                │ 替代z-space mahal                    │
  ├──────────────────┼────────────────┼──────────────────────────────────────┤
  │ EpisodicFlow-    │ L1692-1707     │ osr17f: 新增score_anti_prototype()   │
  │ Classifier       │                │ 几何反射评分,无需训练                │
  ├──────────────────┼────────────────┼──────────────────────────────────────┤
  │ OSRCalibrator    │ L3211-3223     │ osr17f: score_anti_prototype_all()   │
  │                  │                │ batch包装器 + 评分路由               │
  ├──────────────────┼────────────────┼──────────────────────────────────────┤
  │ calibrate()      │ L3460-3463     │ osr17f: flow_hybrid用feat_stats      │
  │                  │ L3553-3580     │ calibrate中改用feat_mahal统计量      │
  ├──────────────────┼────────────────┼──────────────────────────────────────┤
  │ evaluate_osr()   │ L3782-3810     │ osr17f: per-round alpha优化          │
  │                  │                │ feat_mahal统计量, Youden J搜索       │
  ├──────────────────┼────────────────┼──────────────────────────────────────┤
  │ main() Phase 3   │ L4218          │ osr17f: 更新osr_methods列表          │
  │                  │                │ +anti_prototype, +z_reconstruction   │
  │                  │                │ -z_gap                               │
  └──────────────────┴────────────────┴──────────────────────────────────────┘

注: osr17f无需重训Phase 2 (仅Phase 3评分改进)
    删除fewshot_best.pth后重跑即可获得新评分结果。


════════════════════════════════════════════════════════════════════
十四、osr17f 结果 + osr17g BUG修复方案 (2026-04-26)
════════════════════════════════════════════════════════════════════

训练: 与osr17e完全相同 (复用checkpoint, Val Acc 67.38%)
Phase 3评分方法改进: anti_prototype新增, flow_hybrid改feat-mahal, +z_reconstruction

────────────────────────
osr17f OSR结果 (TPR@95%TNR)
────────────────────────

| 方法 | Calib TPR | Test TPR | vs osr17e |
|------|-----------|----------|-----------|
| feat_mahal | 15.57% | 12.54% | 不变 |
| **anti_prototype** | 8.01% | **17.45%** | **NEW, 最佳!** |
| reciprocal | 16.24% | 8.72% | 不变 |
| z_consistency | 3.50% | 4.22% | 不变 |
| z_reconstruction | 2.74% | 5.98% | NEW |
| z_mahal_fusion | 4.75% | 5.09% | 不变 |
| flow_hybrid | 16.08% | **7.38%** | **退步! (was 9.15%)** |
| flow_energy | 6.19% | 5.03% | 不变 |
| ood_head | 18.26% | 10.12% | 不变 |

────────────────────────
关键发现与BUG诊断
────────────────────────

1. anti_prototype: 17.45% test TPR — 但有严重BUG

   Calib 8.01% → Test 17.45% (calib比test差!) 这是异常的。
   根因: score_anti_prototype() 使用 features.mean(dim=0) 作为反射中心。
   已知/未知样本分开评分 → 各自的feat_center不同 → 分数不可比!

   Calib评分流程:
     for c in base_classes: score(class_c_feats)  # feat_center = mean(class_c)
     for c in unknown_classes: score(class_c_feats)  # feat_center = mean(class_c)
   每个类的feat_center都不同, 破坏了分数的一致性。

   Test的17.45%是per-round recalibration偶然得到的好结果, 不是方法本身有效。

   修复: 使用 prototypes.mean() (固定的原型质心) 作为反射中心。

2. flow_hybrid退步: 9.15% → 7.38%

   原因: 从z-space mahal切换到feat-space mahal。
   feat-space mahal单独有12.54% TPR, 但与flow_energy(5%)融合后反而变差。
   可能原因: 两个分数的分布形态差异大, z-normalization无法对齐。

   修复: 恢复到z-space mahal。

3. z_reconstruction: 5.98% test TPR — 边际有用

   分离度: known [-17.49,-8.42] mean=-13.13, unknown [-17.56,-7.87] mean=-12.85
   分离仅 -0.28, 信号很弱。flow对已知/未知都重构得很好。

────────────────────────
osr17g 实施方案 (3项BUG修复 + 1新方法, 无需重训)
────────────────────────

修改1: anti_prototype反射中心BUG修复 (CRITICAL)

  问题: feat_center = features.mean() → 每次评分中心不同
  修复: feat_center = prototypes.mean() → 固定中心
  位置: episodic_trainer.py score_anti_prototype()

  修复前: Calib 8.01% / Test 17.45% (不一致, 不可信)
  预期修复后: Calib ~12-16% / Test ~14-18% (一致, 可信)

修改2: flow_hybrid恢复z-space mahal

  问题: feat-space mahal切换导致退步 9.15%→7.38%
  修复: 恢复score_samples_mahalanobis(z-space)
  位置: score_samples_combined, calibrate(), evaluate_osr()

  预期: 恢复到osr17e的9.15% test TPR

修改3: 新增geo_fusion方法 (anti_prototype + feat_mahal)

  原理: 融合两个最佳方法:
    anti_prototype (17.45% test TPR, 修复后预期14-18%)
    feat_mahalanobis (12.54% test TPR)
  实现: z-normalize + per-round alpha优化 (Youden J)
  位置: 新增score_samples_geo_fusion() + calibrate/evaluate预处理

  预期: 15-20% test TPR (融合增益)

修改4: 更新评估方法列表
  +geo_fusion, 保留所有现有方法

────────────────────────
预期结果 (osr17g)
────────────────────────

| 方法 | osr17f Test TPR | osr17g实际 | 状态 |
|------|-----------------|-----------|------|
| feat_mahal | 12.54% | 12.54% | ✅ 不变 |
| anti_prototype | 17.45% | **8.32%** | ❌ 中心修复反而退步 |
| geo_fusion | - | 9.64% | ⚠️ 不及feat_mahal |
| reciprocal | 8.72% | 8.72% | ✅ 不变 |
| flow_hybrid | - | 崩溃 | ❌ latent_stats缺失 |
| flow_energy | - | 未运行 | ❌ 崩溃后中断 |
| ood_head | - | 未运行 | ❌ 崩溃后中断 |

注: osr17g运行不完整 (flow_hybrid处崩溃), 以上为已完成方法的结果。
    崩溃原因: flow_hybrid恢复z-space mahal后未同步更新calibrate()的latent_stats调用。
    已修复: calibrate()中flow_hybrid条件恢复compute_latent_stats。


════════════════════════════════════════════════════════════════════
十五、osr17g 诊断 + osr17h 修正方案 (2026-04-26)
════════════════════════════════════════════════════════════════════

────────────────────────
关键诊断: anti_prototype中心选择
────────────────────────

实验对比 (TAU22, TPR@95%TNR):

| 中心来源 | Calib TPR | Test TPR | Calib→Test | 评价 |
|---------|-----------|----------|------------|------|
| features.mean() (osr17f) | 8.01% | **17.45%** | -9.44% | test极好但calib差 |
| prototypes.mean() (osr17g) | 18.61% | 8.32% | +10.29% | calib好但test差 |

分析:
  - prototypes.mean(): 固定中心导致过拟合calib数据中的原型分布
  - features.mean(): 为每类样本创建独立参考系，per-round recalibration
    补偿了不一致性，在test上泛化更好
  - 本质: query-dependent center产生了known/unknown间的系统偏差，
    per-round阈值调整利用了这种偏差

结论: 回退到features.mean()。虽然理论上有"不一致性"，但empirical结果证明有效。

────────────────────────
osr17h 修正方案 (2项修改, 无需重训Phase 2)
────────────────────────

修改1: anti_prototype回退到query-dependent center

  features.mean() → prototypes.mean() → features.mean()
  (osr17f)           (osr17g, 退步)     (osr17h, 恢复)

  代码: score_anti_prototype() 恢复 feat_center = features.mean()
  预期: 恢复anti_prototype的17.45% test TPR

修改2: 保留osr17g已修复的两个bug

  - flow_hybrid: calibrate()已恢复compute_latent_stats调用
  - flow_hybrid: 已恢复z-space mahal (从osr17f的feat-mahal退步中恢复)

  这两个修复确保flow_hybrid/flow_energy/ood_head能正常完成。

────────────────────────
Flow OSR保留策略
────────────────────────

当前Flow方法性能排序 (test TPR):

| 方法 | Test TPR | 角色 |
|------|----------|------|
| flow_hybrid | ~9% (osr17e) | Flow+Z-Mahal融合 |
| ood_head | 10.12% | 神经网络OOD头(使用分类置信度) |
| flow_energy | 5.03% | Flow log-likelihood |
| z_consistency | 4.22% | Z空间偏离度 |
| z_reconstruction | 5.98% | Flow重建误差 |

Flow方法虽弱但保留原因:
1. 论文需要展示flow架构的完整评估
2. flow_hybrid的17% calib TPR说明flow有潜在信号
3. 不同的OSR方法适合不同的数据集，flow可能对其他数据集更有效

Flow性能弱的根因(已确认):
- Flow + condition_network太强 → 任何输入都能映射到z≈N(0,I)
- Z空间方法(z_consistency/z_gap)因此失效
- 这是flow训练良好的副产品，不是bug

────────────────────────
预期结果 (osr17h, 完整运行)
────────────────────────

| 方法 | 预期Test TPR | 来源 |
|------|-------------|------|
| anti_prototype | ~17% | 回退query-dependent center |
| feat_mahal | 12.54% | 不变 |
| ood_head | ~10% | 恢复运行 |
| geo_fusion | ~10-14% | anti恢复后可能提升 |
| reciprocal | 8.72% | 不变 |
| flow_hybrid | ~9% | 恢复z-space mahal |
| flow_energy | ~5% | 恢复运行 |

注: osr17h无需重训Phase 2, fewshot_best.pth已存在。
    重跑即可获得完整的10种方法评估结果。
